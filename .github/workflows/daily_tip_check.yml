# Code generated internal/ci/ci_tool.cue; DO NOT EDIT.

"on":
  schedule:
    - cron: 20 3 * * *
  workflow_dispatch: {}
  push:
    branches:
      - ci/test
    tags-ignore:
      - v*
jobs:
  run:
    runs-on: ubuntu-22.04
    defaults:
      run:
        shell: bash
    steps:
      - run: gh workflow run --ref alpha --repo cue-lang/cuelang.org trybot.yml --raw-field scheduled=true
