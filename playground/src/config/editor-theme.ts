import { vscodeLightInit } from '@uiw/codemirror-theme-vscode';

export const vscodeLight = vscodeLightInit({
    settings: {
        lineHighlight: '#f0f0f0b3',
        selectionMatch: '#c2c6cb',
    }
});
